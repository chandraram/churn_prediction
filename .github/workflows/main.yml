# name: Churn Prediction CI/CD

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#     - name: 🧾 Checkout code
#       uses: actions/checkout@v3

#     - name: 🐍 Set up Python
#       uses: actions/setup-python@v4
#       with:
#         python-version: '3.10'

#     - name: 📦 Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install -r requirements.txt

#     - name: 🧠 Run model training
#       run: python train_model.py

#     - name: 🔍 Check FastAPI app (syntax only)
#       run: python -m py_compile main.py

#     - name: 🐳 Build Docker image
#       run: docker build -t churn-api .

#     - name: 🔐 Log in to Docker Hub
#       uses: docker/login-action@v2
#       with:
#         username: rampucsd15
#         password: 1415Pandu

#     - name: 📤 Push Docker image to Docker Hub
#       run: |
#         docker tag churn-api rampucsd15/churn-api:latest
#         docker push rampucsd15/churn-api:latest


name: Churn Prediction CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-dockerize:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout code
        uses: actions/checkout@v3

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🧠 Run model training
        run: python train_model.py

      - name: 🔍 Check FastAPI app (syntax only)
        run: python -m py_compile main.py

      - name: 🔐 Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: rampucsd15
          password: 1415Pandu

      - name: 🐳 Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: rampucsd15/churn-api:latest
